import { useRef } from "react";

export const useCountRenders = () => {
  // eslint-disable-next-line no-unused-vars
  const renders = useRef(0);
  // console.log("Hello Callback renders: ", renders.current++);
};
